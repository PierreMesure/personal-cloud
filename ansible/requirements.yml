---
collections:
  # Keep these versions aligned with the pinned ansible-core in CI.
  - name: hetzner.hcloud
    version: 6.10.0
  - name: community.docker
    version: 5.2.1
  - name: ansible.posix
    version: 2.2.2
